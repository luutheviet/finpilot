# Copy file này thành `.env` rồi điền giá trị thật. KHÔNG commit `.env`.

# OpenAI
OPENAI_API_KEY=sk-proj-replace-me

# Database (khớp với docker-compose.yml)
DB_NAME=finpilot
DB_USER=finpilot
DB_PASSWORD=finpilot
DB_PORT=5432

# Server
SERVER_PORT=8080

# AI cost guardrail (USD) — log WARN khi vượt
FINPILOT_AI_COST_CAP_USD=5.0
